import 'package:flutter/material.dart';
import './screens/welcome_screen.dart';

class PreWelcome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
      body: Welcome(),
    );
  }
}
